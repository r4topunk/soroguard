# Threat Model (STRIDE) — `CBBMQBNHB2FYVZYV7VNHOJHUMTFJLR4PUMRVQYNW6RHIKZO2NQMIBUCV`

Network mainnet · generated at 2026-09-17 · official Stellar template (*STRIDE-template*, developers.stellar.org/docs/build/security-docs/threat-modeling).

Draft generated by soroguard from the deployed WASM. Every claim carries its evidence tier: **A** = bytecode fact, **B** = fact observed on-chain, **C** = inference. The sections the analysis cannot support appear as a declared gap — never filled with generic text. **Human review is mandatory before submission.**

---

## What are we working on?

**Gap to be filled by the team.** soroguard derives this section from the deployed WASM and does not know the contract's business purpose. The descriptive paragraph the template asks for — what the protocol does, who the actors are, what value it holds in custody, which trust assumptions live off-chain — is not derivable from the bytecode and has to be written by the people who built the system. What follows is only the measured technical surface.

### Analyzed object

| Field | Value |
|---|---|
| Contract ID | `CBBMQBNHB2FYVZYV7VNHOJHUMTFJLR4PUMRVQYNW6RHIKZO2NQMIBUCV` |
| Network | mainnet |
| WASM hash | `12fca5a7a96577273b6d4184cf9c984036cda0e8f0594747e7b2933dced37ee6` |
| WASM size | 90057 bytes |
| Generated at | 2026-09-17 (UTC) |
| Call graph soundness | complete — no `call_indirect` in the module |

The whole document describes **this binary**, identified by the hash above — not the repository it supposedly came from. That is the only thing the analysis can assert: source and deployment diverge often, and what runs in production is the WASM.

### How to read the evidence

| Tier | Means | Who can verify |
|---|---|---|
| **A** | bytecode fact: reachability in the deployed WASM's call graph | anyone, re-derivable from the binary |
| **B** | fact observed on-chain: events actually emitted in the observed window | anyone, via RPC |
| **C** | inference — requires human review. Covers threat class, severity and remediation | human review only |

One asymmetry changes how everything below reads: **"does not reach `require_auth`" is a sound negative for this module's call graph** (when that graph is complete) — it says nothing about authorization enforced inside a contract reached through `call`/`try_call`, nor about a custom account's `__check_auth`. **"reaches `put_contract_data`", on the other hand, is an over-approximation** — the write may sit on a branch that entrypoint never executes. That is why every positive claim carries its hop count, and long paths are flagged for confirmation instead of asserted.

### Exported surface (tier A)

96 exported, 96 invocable entrypoints · 38 reach `require_auth*` · 53 reach storage write · 25 reach `call`/`try_call` · 1 reaches self-code replacement. No `__*` export, so the two counts coincide.

| Entrypoint | auth | writes | durability | event | upgrade | cross-call | fanout |
|---|---|---|---|---|---|---|---|
| `adjust_total_accumulated_reward` | yes | yes | `pers`, `inst` | no | no | no | 98 |
| `admin_set_rewards_state` | yes | yes | `pers`, `inst` | no | no | yes | 138 |
| `apply_transfer_ownership` | yes | yes | `inst` | yes | no | no | 48 |
| `apply_upgrade` | yes | yes | `inst` | yes | yes | yes | 59 |
| `backfill_plane_data` | no | no | — | no | no | yes | 119 |
| `claim` | yes | yes | `pers`, `inst` | yes | no | yes | 143 |
| `claim_all_position_fees` | yes | yes | `pers`, `inst` | yes | no | yes | 155 |
| `claim_position_fees` | yes | yes | `pers`, `inst` | yes | no | yes | 155 |
| `claim_protocol_fees` | yes | yes | `inst` | yes | no | yes | 51 |
| `commit_transfer_ownership` | yes | yes | `inst` | yes | no | no | 48 |
| `commit_upgrade` | yes | yes | `inst` | yes | no | no | 47 |
| `contract_name` | no | no | — | no | no | no | 4 |
| `deposit` | yes | yes | `pers`, `inst` | yes | no | yes | 256 |
| `deposit_position` | yes | yes | `pers`, `inst` | yes | no | yes | 256 |
| `estimate_deposit` | no | no | — | no | no | no | 95 |
| `estimate_deposit_position` | no | no | — | no | no | no | 95 |
| `estimate_swap` | no | yes | `pers`, `inst` | no | no | no | 165 |
| `estimate_swap_strict_receive` | no | yes | `pers`, `inst` | no | no | no | 165 |
| `estimate_withdraw_position` | no | no | — | no | no | no | 99 |
| `estimate_working_balance` | no | yes | `?` | no | no | yes | 86 |
| `gauge_add` | yes | yes | `inst` | yes | no | yes | 45 |
| `gauge_remove` | yes | yes | `inst` | yes | no | no | 39 |
| `gauge_schedule_reward` | yes | yes | `?` | yes | no | yes | 53 |
| `gauges_claim` | yes | yes | `pers`, `inst` | yes | no | yes | 75 |
| `gauges_get_reward_info` | no | yes | `pers`, `inst` | no | no | yes | 79 |
| `get_active_liquidity` | no | no | — | no | no | no | 22 |
| `get_all_position_fees` | no | no | — | no | no | no | 78 |
| `get_chunk_bitmap` | no | no | — | no | no | no | 18 |
| `get_chunk_bitmap_batch` | no | no | — | no | no | no | 20 |
| `get_emergency_mode` | no | no | — | no | no | no | 14 |
| `get_fee_fraction` | no | no | — | no | no | no | 19 |
| `get_fee_growth_global_0_x128` | no | no | — | no | no | no | 18 |
| `get_fee_growth_global_1_x128` | no | no | — | no | no | no | 18 |
| `get_future_address` | no | no | — | no | no | no | 20 |
| `get_gauges` | no | no | — | no | no | no | 16 |
| `get_info` | no | no | — | no | no | no | 25 |
| `get_is_killed_claim` | no | no | — | no | no | no | 16 |
| `get_is_killed_deposit` | no | no | — | no | no | no | 16 |
| `get_is_killed_swap` | no | no | — | no | no | no | 16 |
| `get_pools_plane` | no | no | — | no | no | no | 17 |
| `get_position` | no | no | — | no | no | no | 29 |
| `get_position_fees` | no | no | — | no | no | no | 76 |
| `get_privileged_addrs` | no | no | — | no | no | no | 23 |
| `get_protocol_fee_fraction` | no | no | — | no | no | no | 17 |
| `get_protocol_fees` | no | no | — | no | no | no | 23 |
| `get_reserves` | no | no | — | no | no | no | 24 |
| `get_rewards_info` | no | yes | `pers`, `inst` | no | no | yes | 119 |
| `get_rewards_state` | no | no | — | no | no | no | 20 |
| `get_slot0` | no | no | — | no | no | no | 22 |
| `get_tick` | no | no | — | no | no | no | 42 |
| `get_tick_bounds` | no | no | — | no | no | no | 19 |
| `get_tick_spacing` | no | no | — | no | no | no | 19 |
| `get_ticks_batch` | no | no | — | no | no | no | 44 |
| `get_tokens` | no | no | — | no | no | no | 18 |
| `get_total_accumulated_reward` | no | yes | `pers`, `inst` | no | no | no | 82 |
| `get_total_claimed_reward` | no | yes | `pers`, `inst` | no | no | no | 83 |
| `get_total_configured_reward` | no | yes | `pers`, `inst` | no | no | no | 83 |
| `get_total_excluded_shares` | no | no | — | no | no | no | 26 |
| `get_total_raw_liquidity` | no | no | — | no | no | no | 22 |
| `get_total_weighted_liquidity` | no | no | — | no | no | no | 22 |
| `get_unused_reward` | no | yes | `pers`, `inst` | no | no | yes | 102 |
| `get_user_position_snapshot` | no | no | — | no | no | no | 26 |
| `get_user_reward` | no | yes | `pers`, `inst` | no | no | yes | 125 |
| `get_user_shares` | no | no | — | no | no | no | 25 |
| `init_pools_plane` | no | yes | `inst` | no | no | no | 19 |
| `initialize` | no | yes | `inst` | no | no | yes | 152 |
| `initialize_all` | no | yes | `inst` | no | no | yes | 165 |
| `initialize_boost_config` | no | yes | `inst` | no | no | no | 26 |
| `initialize_rewards_config` | no | yes | `inst` | no | no | no | 23 |
| `kill_claim` | yes | yes | `inst` | yes | no | no | 34 |
| `kill_deposit` | yes | yes | `inst` | yes | no | no | 34 |
| `kill_gauges_claim` | yes | yes | `inst` | yes | no | no | 33 |
| `kill_swap` | yes | yes | `inst` | yes | no | no | 34 |
| `pool_type` | no | no | — | no | no | no | 5 |
| `return_unused_reward` | yes | yes | `pers`, `inst` | no | no | yes | 117 |
| `revert_transfer_ownership` | yes | yes | `inst` | yes | no | no | 40 |
| `revert_upgrade` | yes | yes | `inst` | yes | no | no | 37 |
| `set_emergency_mode` | yes | yes | `inst` | yes | no | no | 30 |
| `set_pools_plane` | yes | yes | `inst` | no | no | no | 31 |
| `set_privileged_addrs` | yes | yes | `inst` | yes | no | no | 34 |
| `set_protocol_fee_fraction` | yes | yes | `inst` | yes | no | no | 34 |
| `set_reward_boost_config` | yes | yes | `inst` | no | no | no | 36 |
| `set_rewards_config` | yes | yes | `pers`, `inst` | no | no | no | 97 |
| `set_rewards_state` | yes | yes | `pers`, `inst` | yes | no | yes | 130 |
| `share_id` | no | no | — | no | no | no | 2 |
| `swap` | yes | yes | `pers`, `inst` | yes | no | yes | 185 |
| `swap_strict_receive` | yes | yes | `pers`, `inst` | yes | no | yes | 185 |
| `tick_from_amounts` | no | no | — | no | no | no | 52 |
| `unkill_claim` | yes | yes | `inst` | yes | no | no | 34 |
| `unkill_deposit` | yes | yes | `inst` | yes | no | no | 34 |
| `unkill_gauges_claim` | yes | yes | `inst` | yes | no | no | 33 |
| `unkill_swap` | yes | yes | `inst` | yes | no | no | 34 |
| `version` | no | no | — | no | no | no | 1 |
| `withdraw` | yes | yes | `pers`, `inst` | yes | no | yes | 246 |
| `withdraw_position` | yes | yes | `pers`, `inst` | yes | no | yes | 246 |
| `get_total_shares` | no | no | — | no | no | no | 22 |

In every column, "yes" means it **reaches** the corresponding host function on some call-graph path, not that it always executes it.

**durability** is the `StorageType` of the writes this entrypoint reaches — `temp` (`Temporary`), `pers` (`Persistent`), `inst` (`Instance`). It is read from the deployed binary: in `put_contract_data`/`del_contract_data` the storage type is the last argument, so a literal at the call site is that argument by construction. The three durabilities are not interchangeable — a `Temporary` entry is deleted permanently when it expires and CAP-0066 does not restore it, and `Instance` is one 64 KiB ledger entry loaded in full on every invocation. `?` means the entrypoint writes but the storage type reaches the call computed, typically through a generic helper that takes durability as a parameter; `—` means no write is reached. Read literally at 52 of 56 storage call sites in this module. This column is a description of the contract's storage layout, not a finding: each durability is correct for some data and wrong for other data, and which one this contract holds is not derivable from the bytecode.

### Inferred data stores

- Likely keys (partial read of the data section): `ChunkBitmap`, `ClaimKilled`, `Fee`, `FeeGrowthGlobal0X128`, `FeeGrowthGlobal1X128`, `FullRangeLiquidity`, `GaugeFutureWasm`, `IsKilledDeposit`, `IsKilledSwap`, `Liquidity`, `MaxInitTick`, `MinInitTick`, `Plane`, `Position`, `ProtocolFeeFraction`, `ProtocolFees`, `Reserve0`, `Reserve1`, `Router`, `Slot0` and 9 more.

Durability is **not** attributable to a key from this list. The `StorageType` is readable per call site (see the `durability` column above), but pairing *which key* goes to *which durability* needs dataflow from the key to the call, which this analysis does not do — a single entrypoint routinely writes several keys at different durabilities. Do not assume the durability of any key below.

### On-chain activity observed (tier B)

Window: ledgers 64427432–64467591 (40160 ledgers, ~61.76h).

window of 40160 ledgers (~62 h) — limited by the RPC request budget, not by retention: the collector could not page the whole retained range in the time allowed, and the contracts that emit most events hit this first; baselines are rates over the observed slice, not totals.

| Topic | Occurrences | Ledgers | Events/hour |
|---|---|---|---|
| `update_reserves` | 14149 | 64427434–64467590 | 229.104 |
| `pool_state` | 14075 | 64427434–64467590 | 227.906 |
| `trade` | 14062 | 64427434–64467590 | 227.695 |
| `claim_fees` | 77 | 64427653–64465889 | 1.247 |
| `claim_reward` | 66 | 64428580–64465888 | 1.069 |
| `position_update` | 13 | 64428597–64463357 | 0.21 |
| `deposit_liquidity` | 9 | 64428668–64463357 | 0.146 |
| `withdraw_liquidity` | 4 | 64428597–64463340 | 0.065 |
| `claim_protocol_fee` | 2 | 64451449–64451449 | 0.032 |

### Data flow diagram

```mermaid
%% soroguard — data-flow diagram derived from the deployed WASM
%% contract CBBMQBNHB2FYVZYV7VNHOJHUMTFJLR4PUMRVQYNW6RHIKZO2NQMIBUCV · network mainnet · generated at 2026-09-17
%% call graph: complete across 96 entrypoint(s)
%% legend: reaches X n = there is a path of n hop(s) from the export to host function X.
%% † = path longer than 2 hops. The positive is over-approximate (tier C): it probably goes through a shared helper and the host function may sit on a branch this entrypoint never executes. Confirm before treating it as a finding. Same threshold and same hop count as detect.ts.
flowchart LR
  subgraph tb_externo["Outside the contract — untrusted actors"]
    e_anonimo["Anonymous invoker — any address"]
    e_autorizado["Authenticated invoker — Address required by require_auth* on at least one reachable path"]
    e_observador["Off-chain consumer of the event stream — getEvents"]
  end
  subgraph tb_auth["require_auth* reachable on some path — access control (admin-shaped) and self-authorization (caller authorizing its own address) are both inside; the name-shape split is in the threat model's Spoofing gap"]
    p_adjust_total_accumulated_reward(["adjust_total_accumulated_reward — reaches (hops to host fn): require_auth 1, read 4†, write 2 ⚠ † = via shared helper, tier C"])
    p_admin_set_rewards_state(["admin_set_rewards_state — reaches (hops to host fn): require_auth 1, read 3†, write 2, cross-call 2 ⚠ † = via shared helper, tier C"])
    p_apply_transfer_ownership(["apply_transfer_ownership — reaches (hops to host fn): require_auth 2, read 2, write 2, event 1"])
    p_apply_upgrade(["apply_upgrade — reaches (hops to host fn): require_auth 2, read 2, write 2, event 1, cross-call 1, self-code upgrade 1"])
    p_claim(["claim — reaches (hops to host fn): require_auth 1, read 3†, write 2, event 1, cross-call 2 ⚠ † = via shared helper, tier C"])
    p_claim_all_position_fees(["claim_all_position_fees — reaches (hops to host fn): require_auth 1, read 3†, write 2, event 2, cross-call 3† ⚠ † = via shared helper, tier C"])
    p_claim_position_fees(["claim_position_fees — reaches (hops to host fn): require_auth 1, read 3†, write 2, event 2, cross-call 3† ⚠ † = via shared helper, tier C"])
    p_claim_protocol_fees(["claim_protocol_fees — reaches (hops to host fn): require_auth 1, read 3†, write 2, event 2, cross-call 3† ⚠ † = via shared helper, tier C"])
    p_commit_transfer_ownership(["commit_transfer_ownership — reaches (hops to host fn): require_auth 2, read 3†, write 2, event 1 ⚠ † = via shared helper, tier C"])
    p_commit_upgrade(["commit_upgrade — reaches (hops to host fn): require_auth 2, read 3†, write 1, event 1 ⚠ † = via shared helper, tier C"])
    p_deposit(["deposit — reaches (hops to host fn): require_auth 2, read 4†, write 3†, event 2, cross-call 4† ⚠ † = via shared helper, tier C"])
    p_deposit_position(["deposit_position — reaches (hops to host fn): require_auth 2, read 4†, write 3†, event 2, cross-call 4† ⚠ † = via shared helper, tier C"])
    p_gauge_add(["gauge_add — reaches (hops to host fn): require_auth 1, read 3†, write 2, event 1, cross-call 2 ⚠ † = via shared helper, tier C"])
    p_gauge_remove(["gauge_remove — reaches (hops to host fn): require_auth 1, read 3†, write 2, event 1 ⚠ † = via shared helper, tier C"])
    p_gauge_schedule_reward(["gauge_schedule_reward — reaches (hops to host fn): require_auth 1, read 3†, write 5†, event 1, cross-call 2 ⚠ † = via shared helper, tier C"])
    p_gauges_claim(["gauges_claim — reaches (hops to host fn): require_auth 1, read 2, write 3†, event 1, cross-call 2 ⚠ † = via shared helper, tier C"])
    p_kill_claim(["kill_claim — reaches (hops to host fn): require_auth 1, read 5†, write 3†, event 1 ⚠ † = via shared helper, tier C"])
    p_kill_deposit(["kill_deposit — reaches (hops to host fn): require_auth 1, read 5†, write 3†, event 1 ⚠ † = via shared helper, tier C"])
    p_kill_gauges_claim(["kill_gauges_claim — reaches (hops to host fn): require_auth 1, read 5†, write 2, event 1 ⚠ † = via shared helper, tier C"])
    p_kill_swap(["kill_swap — reaches (hops to host fn): require_auth 1, read 5†, write 3†, event 1 ⚠ † = via shared helper, tier C"])
    p_return_unused_reward(["return_unused_reward — reaches (hops to host fn): require_auth 1, read 3†, write 6†, cross-call 3† ⚠ † = via shared helper, tier C"])
    p_revert_transfer_ownership(["revert_transfer_ownership — reaches (hops to host fn): require_auth 2, read 6†, write 2, event 1 ⚠ † = via shared helper, tier C"])
    p_revert_upgrade(["revert_upgrade — reaches (hops to host fn): require_auth 2, read 6†, write 2, event 1 ⚠ † = via shared helper, tier C"])
    p_set_emergency_mode(["set_emergency_mode — reaches (hops to host fn): require_auth 1, read 5†, write 1, event 1 ⚠ † = via shared helper, tier C"])
    p_set_pools_plane(["set_pools_plane — reaches (hops to host fn): require_auth 1, read 5†, write 3† ⚠ † = via shared helper, tier C"])
    p_set_privileged_addrs(["set_privileged_addrs — reaches (hops to host fn): require_auth 2, read 5†, write 2, event 1 ⚠ † = via shared helper, tier C"])
    p_set_protocol_fee_fraction(["set_protocol_fee_fraction — reaches (hops to host fn): require_auth 1, read 5†, write 3†, event 1 ⚠ † = via shared helper, tier C"])
    p_set_reward_boost_config(["set_reward_boost_config — reaches (hops to host fn): require_auth 1, read 5†, write 3† ⚠ † = via shared helper, tier C"])
    p_set_rewards_config(["set_rewards_config — reaches (hops to host fn): require_auth 1, read 3†, write 1 ⚠ † = via shared helper, tier C"])
    p_set_rewards_state(["set_rewards_state — reaches (hops to host fn): require_auth 1, read 3†, write 2, event 1, cross-call 2 ⚠ † = via shared helper, tier C"])
    p_swap(["swap — reaches (hops to host fn): require_auth 1, read 4†, write 3†, event 2, cross-call 4† ⚠ † = via shared helper, tier C"])
    p_swap_strict_receive(["swap_strict_receive — reaches (hops to host fn): require_auth 1, read 4†, write 3†, event 2, cross-call 4† ⚠ † = via shared helper, tier C"])
    p_unkill_claim(["unkill_claim — reaches (hops to host fn): require_auth 1, read 5†, write 3†, event 1 ⚠ † = via shared helper, tier C"])
    p_unkill_deposit(["unkill_deposit — reaches (hops to host fn): require_auth 1, read 5†, write 3†, event 1 ⚠ † = via shared helper, tier C"])
    p_unkill_gauges_claim(["unkill_gauges_claim — reaches (hops to host fn): require_auth 1, read 5†, write 2, event 1 ⚠ † = via shared helper, tier C"])
    p_unkill_swap(["unkill_swap — reaches (hops to host fn): require_auth 1, read 5†, write 3†, event 1 ⚠ † = via shared helper, tier C"])
    p_withdraw(["withdraw — reaches (hops to host fn): require_auth 2, read 4†, write 3†, event 2, cross-call 4† ⚠ † = via shared helper, tier C"])
    p_withdraw_position(["withdraw_position — reaches (hops to host fn): require_auth 2, read 4†, write 3†, event 2, cross-call 4† ⚠ † = via shared helper, tier C"])
  end
  subgraph tb_aberto["No authorization boundary — no path reaches require_auth* in this module (sound for this call graph)"]
    p_backfill_plane_data(["backfill_plane_data — reaches (hops to host fn): read 4†, cross-call 3† ⚠ † = via shared helper, tier C"])
    p_contract_name(["contract_name — reaches 1 host function(s), none from auth, storage, event, cross-call or upgrade"])
    p_estimate_deposit(["estimate_deposit — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_estimate_deposit_position(["estimate_deposit_position — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_estimate_swap(["estimate_swap — reaches (hops to host fn): read 5†, write 4† ⚠ † = via shared helper, tier C"])
    p_estimate_swap_strict_receive(["estimate_swap_strict_receive — reaches (hops to host fn): read 5†, write 4† ⚠ † = via shared helper, tier C"])
    p_estimate_withdraw_position(["estimate_withdraw_position — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_estimate_working_balance(["estimate_working_balance — reaches (hops to host fn): read 3†, write 4†, cross-call 3† ⚠ † = via shared helper, tier C"])
    p_gauges_get_reward_info(["gauges_get_reward_info — reaches (hops to host fn): read 3†, write 3†, cross-call 1 ⚠ † = via shared helper, tier C"])
    p_get_active_liquidity(["get_active_liquidity — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_all_position_fees(["get_all_position_fees — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_chunk_bitmap(["get_chunk_bitmap — reaches (hops to host fn): read 5† ⚠ † = via shared helper, tier C"])
    p_get_chunk_bitmap_batch(["get_chunk_bitmap_batch — reaches (hops to host fn): read 5† ⚠ † = via shared helper, tier C"])
    p_get_emergency_mode(["get_emergency_mode — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_fee_fraction(["get_fee_fraction — reaches (hops to host fn): read 5† ⚠ † = via shared helper, tier C"])
    p_get_fee_growth_global_0_x128(["get_fee_growth_global_0_x128 — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_fee_growth_global_1_x128(["get_fee_growth_global_1_x128 — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_future_address(["get_future_address — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_gauges(["get_gauges — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_info(["get_info — reaches (hops to host fn): read 5† ⚠ † = via shared helper, tier C"])
    p_get_is_killed_claim(["get_is_killed_claim — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_is_killed_deposit(["get_is_killed_deposit — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_is_killed_swap(["get_is_killed_swap — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_pools_plane(["get_pools_plane — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_position(["get_position — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_position_fees(["get_position_fees — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_privileged_addrs(["get_privileged_addrs — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_protocol_fee_fraction(["get_protocol_fee_fraction — reaches (hops to host fn): read 5† ⚠ † = via shared helper, tier C"])
    p_get_protocol_fees(["get_protocol_fees — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_reserves(["get_reserves — reaches (hops to host fn): read 5† ⚠ † = via shared helper, tier C"])
    p_get_rewards_info(["get_rewards_info — reaches (hops to host fn): read 3†, write 3†, cross-call 2 ⚠ † = via shared helper, tier C"])
    p_get_rewards_state(["get_rewards_state — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_slot0(["get_slot0 — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_tick(["get_tick — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_tick_bounds(["get_tick_bounds — reaches (hops to host fn): read 6† ⚠ † = via shared helper, tier C"])
    p_get_tick_spacing(["get_tick_spacing — reaches (hops to host fn): read 5† ⚠ † = via shared helper, tier C"])
    p_get_ticks_batch(["get_ticks_batch — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_tokens(["get_tokens — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_total_accumulated_reward(["get_total_accumulated_reward — reaches (hops to host fn): read 4†, write 4† ⚠ † = via shared helper, tier C"])
    p_get_total_claimed_reward(["get_total_claimed_reward — reaches (hops to host fn): read 4†, write 5† ⚠ † = via shared helper, tier C"])
    p_get_total_configured_reward(["get_total_configured_reward — reaches (hops to host fn): read 4†, write 5† ⚠ † = via shared helper, tier C"])
    p_get_total_excluded_shares(["get_total_excluded_shares — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_total_raw_liquidity(["get_total_raw_liquidity — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_total_weighted_liquidity(["get_total_weighted_liquidity — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
    p_get_unused_reward(["get_unused_reward — reaches (hops to host fn): read 4†, write 6†, cross-call 3† ⚠ † = via shared helper, tier C"])
    p_get_user_position_snapshot(["get_user_position_snapshot — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_get_user_reward(["get_user_reward — reaches (hops to host fn): read 3†, write 3†, cross-call 2 ⚠ † = via shared helper, tier C"])
    p_get_user_shares(["get_user_shares — reaches (hops to host fn): read 3† ⚠ † = via shared helper, tier C"])
    p_init_pools_plane(["init_pools_plane — reaches (hops to host fn): read 3†, write 4† ⚠ † = via shared helper, tier C"])
    p_initialize(["initialize — reaches (hops to host fn): read 5†, write 3†, cross-call 4† ⚠ † = via shared helper, tier C"])
    p_initialize_all(["initialize_all — reaches (hops to host fn): read 3†, write 3†, cross-call 4† ⚠ † = via shared helper, tier C"])
    p_initialize_boost_config(["initialize_boost_config — reaches (hops to host fn): read 5†, write 4† ⚠ † = via shared helper, tier C"])
    p_initialize_rewards_config(["initialize_rewards_config — reaches (hops to host fn): read 4†, write 3† ⚠ † = via shared helper, tier C"])
    p_pool_type(["pool_type — reaches 1 host function(s), none from auth, storage, event, cross-call or upgrade"])
    p_share_id(["share_id — reaches 1 host function(s), none from auth, storage, event, cross-call or upgrade"])
    p_tick_from_amounts(["tick_from_amounts — reaches 19 host function(s), none from auth, storage, event, cross-call or upgrade"])
    p_version(["version — no host function reached"])
    p_get_total_shares(["get_total_shares — reaches (hops to host fn): read 4† ⚠ † = via shared helper, tier C"])
  end
  subgraph tb_crosscall["Third-party code — reached through call/try_call"]
    x_callee[["Another contract — address resolved at runtime, not determinable from the bytecode"]]
  end
  s_storage[("Contract storage")]
  s_ChunkBitmap[("ChunkBitmap (likely)")]
  s_ClaimKilled[("ClaimKilled (likely)")]
  s_Fee[("Fee (likely)")]
  s_FeeGrowthGlobal0X128[("FeeGrowthGlobal0X128 (likely)")]
  s_FeeGrowthGlobal1X128[("FeeGrowthGlobal1X128 (likely)")]
  s_FullRangeLiquidity[("FullRangeLiquidity (likely)")]
  s_GaugeFutureWasm[("GaugeFutureWasm (likely)")]
  s_IsKilledDeposit[("IsKilledDeposit (likely)")]
  s_IsKilledSwap[("IsKilledSwap (likely)")]
  s_Liquidity[("Liquidity (likely)")]
  s_MaxInitTick[("MaxInitTick (likely)")]
  s_MinInitTick[("MinInitTick (likely)")]
  s_Plane[("Plane (likely)")]
  s_Position[("Position (likely)")]
  s_ProtocolFeeFraction[("ProtocolFeeFraction (likely)")]
  s_ProtocolFees[("ProtocolFees (likely)")]
  s_Reserve0[("Reserve0 (likely)")]
  s_Reserve1[("Reserve1 (likely)")]
  s_Router[("Router (likely)")]
  s_Slot0[("Slot0 (likely)")]
  s_TickChunk[("TickChunk (likely)")]
  s_TickSpacing[("TickSpacing (likely)")]
  s_Token0[("Token0 (likely)")]
  s_Token1[("Token1 (likely)")]
  s_TokenFutureWasm[("TokenFutureWasm (likely)")]
  s_TotalRawLiquidity[("TotalRawLiquidity (likely)")]
  s_TotalWeightedLiquidity[("TotalWeightedLiquidity (likely)")]
  s_User[("User (likely)")]
  s_WordBitmap[("WordBitmap (likely)")]
  e_autorizado -.->|"invokes — require_auth reachable"| p_adjust_total_accumulated_reward
  e_autorizado -.->|"invokes — require_auth reachable"| p_admin_set_rewards_state
  e_autorizado -.->|"invokes — require_auth reachable"| p_apply_transfer_ownership
  e_autorizado -.->|"invokes — require_auth reachable"| p_apply_upgrade
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_backfill_plane_data
  e_autorizado -.->|"invokes — require_auth reachable"| p_claim
  e_autorizado -.->|"invokes — require_auth reachable"| p_claim_all_position_fees
  e_autorizado -.->|"invokes — require_auth reachable"| p_claim_position_fees
  e_autorizado -.->|"invokes — require_auth reachable"| p_claim_protocol_fees
  e_autorizado -.->|"invokes — require_auth reachable"| p_commit_transfer_ownership
  e_autorizado -.->|"invokes — require_auth reachable"| p_commit_upgrade
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_contract_name
  e_autorizado -.->|"invokes — require_auth reachable"| p_deposit
  e_autorizado -.->|"invokes — require_auth reachable"| p_deposit_position
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_estimate_deposit
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_estimate_deposit_position
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_estimate_swap
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_estimate_swap_strict_receive
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_estimate_withdraw_position
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_estimate_working_balance
  e_autorizado -.->|"invokes — require_auth reachable"| p_gauge_add
  e_autorizado -.->|"invokes — require_auth reachable"| p_gauge_remove
  e_autorizado -.->|"invokes — require_auth reachable"| p_gauge_schedule_reward
  e_autorizado -.->|"invokes — require_auth reachable"| p_gauges_claim
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_gauges_get_reward_info
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_active_liquidity
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_all_position_fees
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_chunk_bitmap
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_chunk_bitmap_batch
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_emergency_mode
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_fee_fraction
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_fee_growth_global_0_x128
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_fee_growth_global_1_x128
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_future_address
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_gauges
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_info
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_is_killed_claim
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_is_killed_deposit
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_is_killed_swap
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_pools_plane
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_position
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_position_fees
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_privileged_addrs
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_protocol_fee_fraction
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_protocol_fees
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_reserves
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_rewards_info
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_rewards_state
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_slot0
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_tick
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_tick_bounds
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_tick_spacing
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_ticks_batch
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_tokens
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_total_accumulated_reward
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_total_claimed_reward
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_total_configured_reward
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_total_excluded_shares
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_total_raw_liquidity
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_total_weighted_liquidity
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_unused_reward
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_user_position_snapshot
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_user_reward
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_user_shares
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_init_pools_plane
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_initialize
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_initialize_all
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_initialize_boost_config
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_initialize_rewards_config
  e_autorizado -.->|"invokes — require_auth reachable"| p_kill_claim
  e_autorizado -.->|"invokes — require_auth reachable"| p_kill_deposit
  e_autorizado -.->|"invokes — require_auth reachable"| p_kill_gauges_claim
  e_autorizado -.->|"invokes — require_auth reachable"| p_kill_swap
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_pool_type
  e_autorizado -.->|"invokes — require_auth reachable"| p_return_unused_reward
  e_autorizado -.->|"invokes — require_auth reachable"| p_revert_transfer_ownership
  e_autorizado -.->|"invokes — require_auth reachable"| p_revert_upgrade
  e_autorizado -.->|"invokes — require_auth reachable"| p_set_emergency_mode
  e_autorizado -.->|"invokes — require_auth reachable"| p_set_pools_plane
  e_autorizado -.->|"invokes — require_auth reachable"| p_set_privileged_addrs
  e_autorizado -.->|"invokes — require_auth reachable"| p_set_protocol_fee_fraction
  e_autorizado -.->|"invokes — require_auth reachable"| p_set_reward_boost_config
  e_autorizado -.->|"invokes — require_auth reachable"| p_set_rewards_config
  e_autorizado -.->|"invokes — require_auth reachable"| p_set_rewards_state
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_share_id
  e_autorizado -.->|"invokes — require_auth reachable"| p_swap
  e_autorizado -.->|"invokes — require_auth reachable"| p_swap_strict_receive
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_tick_from_amounts
  e_autorizado -.->|"invokes — require_auth reachable"| p_unkill_claim
  e_autorizado -.->|"invokes — require_auth reachable"| p_unkill_deposit
  e_autorizado -.->|"invokes — require_auth reachable"| p_unkill_gauges_claim
  e_autorizado -.->|"invokes — require_auth reachable"| p_unkill_swap
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_version
  e_autorizado -.->|"invokes — require_auth reachable"| p_withdraw
  e_autorizado -.->|"invokes — require_auth reachable"| p_withdraw_position
  e_anonimo -.->|"invokes — no authorization check within reach (sound for this call graph)"| p_get_total_shares
  s_storage -->|"inferred key — confidence likely"| s_ChunkBitmap
  s_storage -->|"inferred key — confidence likely"| s_ClaimKilled
  s_storage -->|"inferred key — confidence likely"| s_Fee
  s_storage -->|"inferred key — confidence likely"| s_FeeGrowthGlobal0X128
  s_storage -->|"inferred key — confidence likely"| s_FeeGrowthGlobal1X128
  s_storage -->|"inferred key — confidence likely"| s_FullRangeLiquidity
  s_storage -->|"inferred key — confidence likely"| s_GaugeFutureWasm
  s_storage -->|"inferred key — confidence likely"| s_IsKilledDeposit
  s_storage -->|"inferred key — confidence likely"| s_IsKilledSwap
  s_storage -->|"inferred key — confidence likely"| s_Liquidity
  s_storage -->|"inferred key — confidence likely"| s_MaxInitTick
  s_storage -->|"inferred key — confidence likely"| s_MinInitTick
  s_storage -->|"inferred key — confidence likely"| s_Plane
  s_storage -->|"inferred key — confidence likely"| s_Position
  s_storage -->|"inferred key — confidence likely"| s_ProtocolFeeFraction
  s_storage -->|"inferred key — confidence likely"| s_ProtocolFees
  s_storage -->|"inferred key — confidence likely"| s_Reserve0
  s_storage -->|"inferred key — confidence likely"| s_Reserve1
  s_storage -->|"inferred key — confidence likely"| s_Router
  s_storage -->|"inferred key — confidence likely"| s_Slot0
  s_storage -->|"inferred key — confidence likely"| s_TickChunk
  s_storage -->|"inferred key — confidence likely"| s_TickSpacing
  s_storage -->|"inferred key — confidence likely"| s_Token0
  s_storage -->|"inferred key — confidence likely"| s_Token1
  s_storage -->|"inferred key — confidence likely"| s_TokenFutureWasm
  s_storage -->|"inferred key — confidence likely"| s_TotalRawLiquidity
  s_storage -->|"inferred key — confidence likely"| s_TotalWeightedLiquidity
  s_storage -->|"inferred key — confidence likely"| s_User
  s_storage -->|"inferred key — confidence likely"| s_WordBitmap
  p_adjust_total_accumulated_reward -->|"reaches read and write"| s_storage
  p_admin_set_rewards_state -->|"reaches read and write"| s_storage
  p_apply_transfer_ownership -->|"reaches read and write"| s_storage
  p_apply_upgrade -->|"reaches read and write"| s_storage
  p_backfill_plane_data -->|"reaches read"| s_storage
  p_claim -->|"reaches read and write"| s_storage
  p_claim_all_position_fees -->|"reaches read and write"| s_storage
  p_claim_position_fees -->|"reaches read and write"| s_storage
  p_claim_protocol_fees -->|"reaches read and write"| s_storage
  p_commit_transfer_ownership -->|"reaches read and write"| s_storage
  p_commit_upgrade -->|"reaches read and write"| s_storage
  p_deposit -->|"reaches read and write"| s_storage
  p_deposit_position -->|"reaches read and write"| s_storage
  p_estimate_deposit -->|"reaches read"| s_storage
  p_estimate_deposit_position -->|"reaches read"| s_storage
  p_estimate_swap -->|"reaches read and write"| s_storage
  p_estimate_swap_strict_receive -->|"reaches read and write"| s_storage
  p_estimate_withdraw_position -->|"reaches read"| s_storage
  p_estimate_working_balance -->|"reaches read and write"| s_storage
  p_gauge_add -->|"reaches read and write"| s_storage
  p_gauge_remove -->|"reaches read and write"| s_storage
  p_gauge_schedule_reward -->|"reaches read and write"| s_storage
  p_gauges_claim -->|"reaches read and write"| s_storage
  p_gauges_get_reward_info -->|"reaches read and write"| s_storage
  p_get_active_liquidity -->|"reaches read"| s_storage
  p_get_all_position_fees -->|"reaches read"| s_storage
  p_get_chunk_bitmap -->|"reaches read"| s_storage
  p_get_chunk_bitmap_batch -->|"reaches read"| s_storage
  p_get_emergency_mode -->|"reaches read"| s_storage
  p_get_fee_fraction -->|"reaches read"| s_storage
  p_get_fee_growth_global_0_x128 -->|"reaches read"| s_storage
  p_get_fee_growth_global_1_x128 -->|"reaches read"| s_storage
  p_get_future_address -->|"reaches read"| s_storage
  p_get_gauges -->|"reaches read"| s_storage
  p_get_info -->|"reaches read"| s_storage
  p_get_is_killed_claim -->|"reaches read"| s_storage
  p_get_is_killed_deposit -->|"reaches read"| s_storage
  p_get_is_killed_swap -->|"reaches read"| s_storage
  p_get_pools_plane -->|"reaches read"| s_storage
  p_get_position -->|"reaches read"| s_storage
  p_get_position_fees -->|"reaches read"| s_storage
  p_get_privileged_addrs -->|"reaches read"| s_storage
  p_get_protocol_fee_fraction -->|"reaches read"| s_storage
  p_get_protocol_fees -->|"reaches read"| s_storage
  p_get_reserves -->|"reaches read"| s_storage
  p_get_rewards_info -->|"reaches read and write"| s_storage
  p_get_rewards_state -->|"reaches read"| s_storage
  p_get_slot0 -->|"reaches read"| s_storage
  p_get_tick -->|"reaches read"| s_storage
  p_get_tick_bounds -->|"reaches read"| s_storage
  p_get_tick_spacing -->|"reaches read"| s_storage
  p_get_ticks_batch -->|"reaches read"| s_storage
  p_get_tokens -->|"reaches read"| s_storage
  p_get_total_accumulated_reward -->|"reaches read and write"| s_storage
  p_get_total_claimed_reward -->|"reaches read and write"| s_storage
  p_get_total_configured_reward -->|"reaches read and write"| s_storage
  p_get_total_excluded_shares -->|"reaches read"| s_storage
  p_get_total_raw_liquidity -->|"reaches read"| s_storage
  p_get_total_weighted_liquidity -->|"reaches read"| s_storage
  p_get_unused_reward -->|"reaches read and write"| s_storage
  p_get_user_position_snapshot -->|"reaches read"| s_storage
  p_get_user_reward -->|"reaches read and write"| s_storage
  p_get_user_shares -->|"reaches read"| s_storage
  p_init_pools_plane -->|"reaches read and write"| s_storage
  p_initialize -->|"reaches read and write"| s_storage
  p_initialize_all -->|"reaches read and write"| s_storage
  p_initialize_boost_config -->|"reaches read and write"| s_storage
  p_initialize_rewards_config -->|"reaches read and write"| s_storage
  p_kill_claim -->|"reaches read and write"| s_storage
  p_kill_deposit -->|"reaches read and write"| s_storage
  p_kill_gauges_claim -->|"reaches read and write"| s_storage
  p_kill_swap -->|"reaches read and write"| s_storage
  p_return_unused_reward -->|"reaches read and write"| s_storage
  p_revert_transfer_ownership -->|"reaches read and write"| s_storage
  p_revert_upgrade -->|"reaches read and write"| s_storage
  p_set_emergency_mode -->|"reaches read and write"| s_storage
  p_set_pools_plane -->|"reaches read and write"| s_storage
  p_set_privileged_addrs -->|"reaches read and write"| s_storage
  p_set_protocol_fee_fraction -->|"reaches read and write"| s_storage
  p_set_reward_boost_config -->|"reaches read and write"| s_storage
  p_set_rewards_config -->|"reaches read and write"| s_storage
  p_set_rewards_state -->|"reaches read and write"| s_storage
  p_swap -->|"reaches read and write"| s_storage
  p_swap_strict_receive -->|"reaches read and write"| s_storage
  p_unkill_claim -->|"reaches read and write"| s_storage
  p_unkill_deposit -->|"reaches read and write"| s_storage
  p_unkill_gauges_claim -->|"reaches read and write"| s_storage
  p_unkill_swap -->|"reaches read and write"| s_storage
  p_withdraw -->|"reaches read and write"| s_storage
  p_withdraw_position -->|"reaches read and write"| s_storage
  p_get_total_shares -->|"reaches read"| s_storage
  p_admin_set_rewards_state -.->|"reaches call / try_call"| x_callee
  p_apply_upgrade -.->|"reaches call"| x_callee
  p_backfill_plane_data -.->|"reaches call"| x_callee
  p_claim -.->|"reaches call / try_call"| x_callee
  p_claim_all_position_fees -.->|"reaches call"| x_callee
  p_claim_position_fees -.->|"reaches call"| x_callee
  p_claim_protocol_fees -.->|"reaches call"| x_callee
  p_deposit -.->|"reaches call / try_call"| x_callee
  p_deposit_position -.->|"reaches call / try_call"| x_callee
  p_estimate_working_balance -.->|"reaches call / try_call"| x_callee
  p_gauge_add -.->|"reaches call"| x_callee
  p_gauge_schedule_reward -.->|"reaches call"| x_callee
  p_gauges_claim -.->|"reaches call"| x_callee
  p_gauges_get_reward_info -.->|"reaches call"| x_callee
  p_get_rewards_info -.->|"reaches call / try_call"| x_callee
  p_get_unused_reward -.->|"reaches call"| x_callee
  p_get_user_reward -.->|"reaches call / try_call"| x_callee
  p_initialize -.->|"reaches call"| x_callee
  p_initialize_all -.->|"reaches call"| x_callee
  p_return_unused_reward -.->|"reaches call"| x_callee
  p_set_rewards_state -.->|"reaches call / try_call"| x_callee
  p_swap -.->|"reaches call"| x_callee
  p_swap_strict_receive -.->|"reaches call"| x_callee
  p_withdraw -.->|"reaches call / try_call"| x_callee
  p_withdraw_position -.->|"reaches call / try_call"| x_callee
  p_apply_transfer_ownership -.->|"reaches contract_event"| e_observador
  p_apply_upgrade -.->|"reaches contract_event"| e_observador
  p_claim -.->|"reaches contract_event"| e_observador
  p_claim_all_position_fees -.->|"reaches contract_event"| e_observador
  p_claim_position_fees -.->|"reaches contract_event"| e_observador
  p_claim_protocol_fees -.->|"reaches contract_event"| e_observador
  p_commit_transfer_ownership -.->|"reaches contract_event"| e_observador
  p_commit_upgrade -.->|"reaches contract_event"| e_observador
  p_deposit -.->|"reaches contract_event"| e_observador
  p_deposit_position -.->|"reaches contract_event"| e_observador
  p_gauge_add -.->|"reaches contract_event"| e_observador
  p_gauge_remove -.->|"reaches contract_event"| e_observador
  p_gauge_schedule_reward -.->|"reaches contract_event"| e_observador
  p_gauges_claim -.->|"reaches contract_event"| e_observador
  p_kill_claim -.->|"reaches contract_event"| e_observador
  p_kill_deposit -.->|"reaches contract_event"| e_observador
  p_kill_gauges_claim -.->|"reaches contract_event"| e_observador
  p_kill_swap -.->|"reaches contract_event"| e_observador
  p_revert_transfer_ownership -.->|"reaches contract_event"| e_observador
  p_revert_upgrade -.->|"reaches contract_event"| e_observador
  p_set_emergency_mode -.->|"reaches contract_event"| e_observador
  p_set_privileged_addrs -.->|"reaches contract_event"| e_observador
  p_set_protocol_fee_fraction -.->|"reaches contract_event"| e_observador
  p_set_rewards_state -.->|"reaches contract_event"| e_observador
  p_swap -.->|"reaches contract_event"| e_observador
  p_swap_strict_receive -.->|"reaches contract_event"| e_observador
  p_unkill_claim -.->|"reaches contract_event"| e_observador
  p_unkill_deposit -.->|"reaches contract_event"| e_observador
  p_unkill_gauges_claim -.->|"reaches contract_event"| e_observador
  p_unkill_swap -.->|"reaches contract_event"| e_observador
  p_withdraw -.->|"reaches contract_event"| e_observador
  p_withdraw_position -.->|"reaches contract_event"| e_observador
  classDef proc fill:#eaf2fb,stroke:#3b6ea5,color:#10243a
  class p_adjust_total_accumulated_reward,p_admin_set_rewards_state,p_apply_transfer_ownership,p_apply_upgrade,p_backfill_plane_data,p_claim,p_claim_all_position_fees,p_claim_position_fees,p_claim_protocol_fees,p_commit_transfer_ownership,p_commit_upgrade,p_contract_name,p_deposit,p_deposit_position,p_estimate_deposit,p_estimate_deposit_position,p_estimate_swap,p_estimate_swap_strict_receive,p_estimate_withdraw_position,p_estimate_working_balance,p_gauge_add,p_gauge_remove,p_gauge_schedule_reward,p_gauges_claim,p_gauges_get_reward_info,p_get_active_liquidity,p_get_all_position_fees,p_get_chunk_bitmap,p_get_chunk_bitmap_batch,p_get_emergency_mode,p_get_fee_fraction,p_get_fee_growth_global_0_x128,p_get_fee_growth_global_1_x128,p_get_future_address,p_get_gauges,p_get_info,p_get_is_killed_claim,p_get_is_killed_deposit,p_get_is_killed_swap,p_get_pools_plane,p_get_position,p_get_position_fees,p_get_privileged_addrs,p_get_protocol_fee_fraction,p_get_protocol_fees,p_get_reserves,p_get_rewards_info,p_get_rewards_state,p_get_slot0,p_get_tick,p_get_tick_bounds,p_get_tick_spacing,p_get_ticks_batch,p_get_tokens,p_get_total_accumulated_reward,p_get_total_claimed_reward,p_get_total_configured_reward,p_get_total_excluded_shares,p_get_total_raw_liquidity,p_get_total_weighted_liquidity,p_get_unused_reward,p_get_user_position_snapshot,p_get_user_reward,p_get_user_shares,p_init_pools_plane,p_initialize,p_initialize_all,p_initialize_boost_config,p_initialize_rewards_config,p_kill_claim,p_kill_deposit,p_kill_gauges_claim,p_kill_swap,p_pool_type,p_return_unused_reward,p_revert_transfer_ownership,p_revert_upgrade,p_set_emergency_mode,p_set_pools_plane,p_set_privileged_addrs,p_set_protocol_fee_fraction,p_set_reward_boost_config,p_set_rewards_config,p_set_rewards_state,p_share_id,p_swap,p_swap_strict_receive,p_tick_from_amounts,p_unkill_claim,p_unkill_deposit,p_unkill_gauges_claim,p_unkill_swap,p_version,p_withdraw,p_withdraw_position,p_get_total_shares proc
  classDef store fill:#fdf3e0,stroke:#b8860b,color:#3a2c10
  class s_storage,s_ChunkBitmap,s_ClaimKilled,s_Fee,s_FeeGrowthGlobal0X128,s_FeeGrowthGlobal1X128,s_FullRangeLiquidity,s_GaugeFutureWasm,s_IsKilledDeposit,s_IsKilledSwap,s_Liquidity,s_MaxInitTick,s_MinInitTick,s_Plane,s_Position,s_ProtocolFeeFraction,s_ProtocolFees,s_Reserve0,s_Reserve1,s_Router,s_Slot0,s_TickChunk,s_TickSpacing,s_Token0,s_Token1,s_TokenFutureWasm,s_TotalRawLiquidity,s_TotalWeightedLiquidity,s_User,s_WordBitmap store
  classDef ext fill:#f2f2f2,stroke:#7a7a7a,color:#222222
  class e_anonimo,e_autorizado,e_observador ext
  classDef outro fill:#fdeaea,stroke:#c05252,color:#3a1010
  class x_callee outro
```

The diagram comes straight out of the analysis: each `process` is a module export, each `store` is an inferred storage key, and each edge marked as a boundary is a point where execution crosses outside the contract (`call`/`try_call`) or where authorization is required.

| Trust boundary | Contained nodes |
|---|---|
| Outside the contract — untrusted actors | `e_anonimo`, `e_autorizado`, `e_observador` |
| require_auth* reachable on some path — access control (admin-shaped) and self-authorization (caller authorizing its own address) are both inside; the name-shape split is in the threat model's Spoofing gap | `p_adjust_total_accumulated_reward`, `p_admin_set_rewards_state`, `p_apply_transfer_ownership`, `p_apply_upgrade`, `p_claim`, `p_claim_all_position_fees`, `p_claim_position_fees`, `p_claim_protocol_fees`, `p_commit_transfer_ownership`, `p_commit_upgrade`, `p_deposit`, `p_deposit_position`, `p_gauge_add`, `p_gauge_remove`, `p_gauge_schedule_reward`, `p_gauges_claim`, `p_kill_claim`, `p_kill_deposit`, `p_kill_gauges_claim`, `p_kill_swap`, `p_return_unused_reward`, `p_revert_transfer_ownership`, `p_revert_upgrade`, `p_set_emergency_mode`, `p_set_pools_plane`, `p_set_privileged_addrs`, `p_set_protocol_fee_fraction`, `p_set_reward_boost_config`, `p_set_rewards_config`, `p_set_rewards_state`, `p_swap`, `p_swap_strict_receive`, `p_unkill_claim`, `p_unkill_deposit`, `p_unkill_gauges_claim`, `p_unkill_swap`, `p_withdraw`, `p_withdraw_position` |
| No authorization boundary — no path reaches require_auth* in this module (sound for this call graph) | `p_backfill_plane_data`, `p_contract_name`, `p_estimate_deposit`, `p_estimate_deposit_position`, `p_estimate_swap`, `p_estimate_swap_strict_receive`, `p_estimate_withdraw_position`, `p_estimate_working_balance`, `p_gauges_get_reward_info`, `p_get_active_liquidity`, `p_get_all_position_fees`, `p_get_chunk_bitmap`, `p_get_chunk_bitmap_batch`, `p_get_emergency_mode`, `p_get_fee_fraction`, `p_get_fee_growth_global_0_x128`, `p_get_fee_growth_global_1_x128`, `p_get_future_address`, `p_get_gauges`, `p_get_info`, `p_get_is_killed_claim`, `p_get_is_killed_deposit`, `p_get_is_killed_swap`, `p_get_pools_plane`, `p_get_position`, `p_get_position_fees`, `p_get_privileged_addrs`, `p_get_protocol_fee_fraction`, `p_get_protocol_fees`, `p_get_reserves`, `p_get_rewards_info`, `p_get_rewards_state`, `p_get_slot0`, `p_get_tick`, `p_get_tick_bounds`, `p_get_tick_spacing`, `p_get_ticks_batch`, `p_get_tokens`, `p_get_total_accumulated_reward`, `p_get_total_claimed_reward`, `p_get_total_configured_reward`, `p_get_total_excluded_shares`, `p_get_total_raw_liquidity`, `p_get_total_weighted_liquidity`, `p_get_unused_reward`, `p_get_user_position_snapshot`, `p_get_user_reward`, `p_get_user_shares`, `p_init_pools_plane`, `p_initialize`, `p_initialize_all`, `p_initialize_boost_config`, `p_initialize_rewards_config`, `p_pool_type`, `p_share_id`, `p_tick_from_amounts`, `p_version`, `p_get_total_shares` |
| Third-party code — reached through call/try_call | `x_callee` |

**How to read the authorization boundary (tier C, name-shape heuristic).** The boundary says only that `require_auth*` is reachable, and that single label covers two different mechanisms. **Access control** — 18 admin-shaped nodes (`admin_set_rewards_state`, `apply_transfer_ownership`, `apply_upgrade`, `commit_transfer_ownership`, `commit_upgrade`, `kill_claim` and 12 more): there the authorized `Address` is a privileged role, and the review has to trace its custody (multisig or a single key). **Self-authorization** — 20 user-shaped nodes (`adjust_total_accumulated_reward`, `claim`, `claim_all_position_fees`, `claim_position_fees`, `claim_protocol_fees`, `deposit` and 14 more): there `require_auth` is the caller authorizing their own address, the expected shape of a user operation (`swap`, `deposit`, `withdraw`), with no privileged key behind it and nothing to trace. Reading the whole boundary as access control overstates it; reading it as self-authorization understates it. The split comes from the name, not from the bytecode: the call graph never shows *whose* `Address` is authorized.

---

## What can go wrong?

### STRIDE reminders

| Mnemonic Threat | Definition | Question |
|---|---|---|
| **S**poofing | The ability to impersonate another user or system component to gain unauthorized access. | Is the user who they say they are? |
| **T**ampering | Unauthorized alteration of data or code. | Has the data or code been modified in some way? |
| **R**epudiation | The ability for a system or user to deny having taken a certain action. | Is there enough data to "prove" the user took the action if they were to deny it? |
| **I**nformation Disclosure | The over-sharing of data expected to be kept private. | Is there anywhere where excessive data is being shared or controls are not properly in place to protect private information? |
| **D**enial of Service | The ability for an attacker to negatively affect the availability of a system. | Can someone, without authorization, impact the availability of the service or business? |
| **E**levation of Privilege | The ability for an attacker to gain additional privileges and roles beyond what they initially were granted. | Are there ways for a user, without proper authentication and authorization to gain access to additional privileges, either through standard or illegitimate means? |

6 derived threats — 1 High, 5 Medium. 5 are flagged for human confirmation before counting as a finding. 4 letters have no derivable finding and are declared below as a gap.

### Threat table

| Threat | Issues |
|---|---|
| **S**poofing | _No threat derivable from the bytecode._ Declared gap — not derivable from the bytecode. The call graph shows *whether* a path reaches `require_auth`; never *who* the verified `Address` is, who holds that address's key, nor how the client that builds the transaction authenticates the user. Identity lives outside the contract. Measured on soroguard's calibration corpus (75 mainnet contracts, `docs/CALIBRACAO.md`): zero derivable findings in 100% of them — this is a structural limit of bytecode analysis, not a detector failure on this contract. Requires manual review of the off-chain flow: custody of the privileged keys (multisig or a single key?), authentication of the frontend/backend that signs, and whether any address with an administrative role is a shared account. Where identity is asserted in this contract: 38 invocable entrypoints reach `require_auth*`. Access control (18 admin-shaped, key-holder trace required): `admin_set_rewards_state`, `apply_transfer_ownership`, `apply_upgrade`, `commit_transfer_ownership`, `commit_upgrade`, `kill_claim` and 12 more — those are the calls whose `Address` is a privileged role and has to be traced back to a key holder: custody, multisig or a single key. Self-authorization (20 user-shaped, no key-holder trace): `adjust_total_accumulated_reward`, `claim`, `claim_all_position_fees`, `claim_position_fees`, `claim_protocol_fees`, `deposit` and 14 more — there `require_auth` is the caller authorizing their own address (`swap`, `deposit`, `withdraw` are the usual shape), which is a user operation, not access control; there is no privileged key behind it to trace. This split is a name-shape heuristic (tier C), not a bytecode fact — the bytecode shows that `require_auth*` is reached, never *whose* address is authorized. Check it against the signatures before using the split as a work list. The module does not export `__check_auth`, so the signature check is the host's, not this contract's. |
| **T**ampering | _No threat derivable from the bytecode._ Declared gap — no finding derived for this contract. Three detectors feed this letter and none fired: `write-before-auth` (compares bytecode offsets *within the same body*), `host-prng-in-value-path` (host PRNG on a path that changes state or calls out) and `vulnerable-sdk` when the advisory is not about authorization. The write-before-auth detector compares offsets *inside a single body* and found no body where a write precedes the first `require_auth`. It cannot see the ordering when the write and the authorization live in different functions, so this is absence of signal, not a demonstration that the ordering is correct. Watch the classification: state tampering through missing authorization lives under Elevation (5 findings), not here. What still requires manual review: validation of the arguments entering the entrypoints, and trust in data coming from another contract (oracle, router) — neither is derivable from reachability. |
| **R**epudiation | **Repudiate.1** — 11 of 43 state-changing entrypoints emit no event · tiers A+C · severity High (C) |
| **I**nformation Disclosure | _No threat derivable from the bytecode._ Declared gap — not derivable from the bytecode. All ledger state in Soroban is public by construction, so "excessive disclosure" is a question about *which data the protocol chose to put on-chain* — a product decision the WASM does not record. The bytecode also does not say what the arguments and the event topics mean. Measured on soroguard's calibration corpus (75 mainnet contracts, `docs/CALIBRACAO.md`): zero derivable findings in 100% of them — a structural limit. Requires manual review: which fields go into storage and into event topics, and whether any of them is data that should not be public or that gives an advantage to whoever reads the ledger before the transaction settles. The concrete surface to review in this contract: inferred storage keys `ChunkBitmap`, `ClaimKilled`, `Fee`, `FeeGrowthGlobal0X128`, `FeeGrowthGlobal1X128`, `FullRangeLiquidity` and 23 more; declared event topics `claim_fees`. Those are the fields that end up readable on a public ledger. |
| **D**enial of Service | _No threat derivable from the bytecode._ Declared gap — no finding derived for this contract. The detector for this letter is `archival-risk`. 90 exports reach the `extend_*_ttl` family, the nearest at 2 hops (`adjust_total_accumulated_reward`, `admin_set_rewards_state`, `apply_transfer_ownership`, `apply_upgrade`, `backfill_plane_data`, `claim` and 84 more), and the detector only fires when none does. Read that count as an over-approximation: *reaching* is not *executing* — measured on the calibration corpus, ~84% of the bare positives go through a shared helper the entrypoint never runs (`docs/CALIBRACAO.md`), and read-only entrypoints land in this list for exactly that reason. If those renewal paths do not run during the contract's normal operation, the archival risk still exists and the bytecode does not show it. What is not derivable from the call graph: resource exhaustion from large input (ledger CPU/memory limits), dependency on the liveness of a contract reached through `call`, and an administrative `pause`/`kill` able to freeze the system. These require manual review. |
| **E**levation of Privilege | **Elevation.1** — `init_pools_plane` reaches state initialization without requiring authorization · tiers A+C · severity Medium (C) · ⚠ confirm before treating as a finding<br><br>**Elevation.2** — `initialize` reaches state initialization without requiring authorization · tiers A+C · severity Medium (C) · ⚠ confirm before treating as a finding<br><br>**Elevation.3** — `initialize_all` reaches state initialization without requiring authorization · tiers A+C · severity Medium (C) · ⚠ confirm before treating as a finding<br><br>**Elevation.4** — `initialize_boost_config` reaches state initialization without requiring authorization · tiers A+C · severity Medium (C) · ⚠ confirm before treating as a finding<br><br>**Elevation.5** — `initialize_rewards_config` reaches state initialization without requiring authorization · tiers A+C · severity Medium (C) · ⚠ confirm before treating as a finding |

The template asks for at least one issue per letter. Letters without a finding appear as a declared gap, not filled in: the tool reads bytecode, and what it cannot derive from there it says it could not. An honestly empty field is verifiable; a field filled with boilerplate is not.

### Threat details

#### Repudiate.1 — 11 of 43 state-changing entrypoints emit no event

| | |
|---|---|
| Class | `silent-mutation` |
| Target | the whole contract |
| Severity | High — **tier C**, a risk judgement, not a bytecode fact |
| Soundness | module call graph complete — sound negative for this module's call graph (authorization enforced in a called contract or in `__check_auth` is not visible here) |

**Evidence**

- **[A]** *(bytecode fact)* — Reach put_contract_data and do not reach contract_event: set_reward_boost_config, set_rewards_config, adjust_total_accumulated_reward, admin_set_rewards_state, init_pools_plane, initialize, initialize_all, initialize_boost_config, initialize_rewards_config, return_unused_reward, set_pools_plane.
- **[A]** *(bytecode fact)* — Of those, upgrade-capable or admin/permission-shaped by name (listed first above): set_reward_boost_config, set_rewards_config.
- **[C]** *(inference — requires human review)* — 10 read-shaped entrypoints that reach a write through a shared helper were excluded from the count (both numerator and denominator): estimate_swap, estimate_swap_strict_receive, estimate_working_balance, gauges_get_reward_info, get_rewards_info, get_total_accumulated_reward, get_total_claimed_reward, get_total_configured_reward, get_unused_reward, get_user_reward. Emitting events from a quoting getter is not the remediation.
- **[C]** *(inference — requires human review)* — Severity rule applied (class of the silent action, not the silent/state-changing fraction): High when any silent entrypoint is upgrade-capable or admin/permission-shaped by name; Low when every silent entrypoint is an init-shaped one-shot; Medium otherwise. Here: deciding entrypoints = set_reward_boost_config, set_rewards_config → High.
- **[C]** *(inference — requires human review)* — Without an event there is no off-chain proof that the action happened, and the change is only detectable by state diff — which makes real-time monitoring of those actions unfeasible.

#### Elevation.1 – Elevation.5 — `initialization-front-running` in 5 entrypoints

These 5 findings are the same shape: family `init`, class `initialization-front-running`, one per entrypoint. Repeating the block 5 times would bury the rest of the document without adding a fact, so what differs per entrypoint is in the table below, and the evidence and the remediation they share are stated once. Each ID stays individual: its row is its anchor, and the threat table and the remediations keep every id.

| ID | Entrypoint | Evidence tiers | Severity | Hops to the write | `has_contract_data` | On-chain probe |
|---|---|---|---|---|---|---|
| **Elevation.1** | `init_pools_plane` | [A]+[C] | Medium | 4 | reachable | guarded |
| **Elevation.2** | `initialize` | [A]+[C] | Medium | 3 | reachable | guarded |
| **Elevation.3** | `initialize_all` | [A]+[C] | Medium | 3 | reachable | guarded |
| **Elevation.4** | `initialize_boost_config` | [A]+[C] | Medium | 4 | reachable | guarded |
| **Elevation.5** | `initialize_rewards_config` | [A]+[C] | Medium | 3 | reachable | guarded |

**Per-ID anchors:** `Elevation.1` · `Elevation.2` · `Elevation.3` · `Elevation.4` · `Elevation.5`. Each row above is the anchor for its id — the monitor ids `<ThreatID>.M.<n>` in the monitoring plan resolve to them.

| | |
|---|---|
| Class | `initialization-front-running` |
| Severity | Medium — **tier C**, a risk judgement, not a bytecode fact |
| Soundness | call graph complete in the subgraphs of the listed entrypoints — sound negative for this module's call graph (authorization enforced in a called contract or in `__check_auth` is not visible here) |

**Evidence** — condensed: the reachable set, the hop count and the applied severity rule are per entrypoint and are in the table above. What follows is the evidence the 5 findings share, stated once.

- **[A]** *(bytecode fact)* — No path reaches require_auth or require_auth_for_args — sound negative.
- **[A]** *(bytecode fact)* — `has_contract_data` IS reachable from this export.
- **[C]** *(inference — requires human review)* — A reachable `has_contract_data` is, in the idiomatic pattern, an already-initialized guard. Whether the guard covers THIS path, and whether it aborts, does not follow from reachability.
- **[C]** *(inference — requires human review)* — The contract does not export `__constructor`, so initialization is a transaction separate from the deploy (CAP-0058). Between one and the other, any address can initialize first and take the privileged roles.

**Shared remediation.** The same actions apply to every entrypoint listed; they stay numbered per id, from `Elevation.1.R.1` to `Elevation.5.R.1`, in *What are we going to do about it*.

> **Tier B/C note.** Observed traffic (42457 events over ~61.76 h) indicates this instance is already initialized, so the tier C wording above describes a window that has most likely already closed. The residual risk is **re-initialization**, and that depends on a guard the bytecode cannot show: `has_contract_data` IS in the reachable set of `init_pools_plane`, `initialize`, `initialize_all`, `initialize_boost_config` and `initialize_rewards_config` — a likely already-initialized guard when present, though reachability does not prove it covers this path.

> **Init probe (tier B).**
>
> - `init_pools_plane` — Probe (unsigned simulateTransaction, ledger 64467591): **guarded** — simulation reverted with Error(Contract, #202) = `ConcentratedPoolError::PlaneAlreadyInitialized`, an already-initialized guard: the one-shot initializer has already fired on this instance.
> - `initialize` — Probe (unsigned simulateTransaction, ledger 64467591): **guarded** — simulation reverted with Error(Contract, #201) = `ConcentratedPoolError::PoolAlreadyInitialized`, an already-initialized guard: the one-shot initializer has already fired on this instance.
> - `initialize_all` — Probe (unsigned simulateTransaction, ledger 64467591): **guarded** — simulation reverted with Error(Contract, #202) = `ConcentratedPoolError::PlaneAlreadyInitialized`, an already-initialized guard: the one-shot initializer has already fired on this instance.
> - `initialize_boost_config` — Probe (unsigned simulateTransaction, ledger 64467591): **guarded** — simulation reverted with Error(Contract, #203) = `ConcentratedPoolError::RewardsAlreadyInitialized`, an already-initialized guard: the one-shot initializer has already fired on this instance.
> - `initialize_rewards_config` — Probe (unsigned simulateTransaction, ledger 64467591): **guarded** — simulation reverted with Error(Contract, #203) = `ConcentratedPoolError::RewardsAlreadyInitialized`, an already-initialized guard: the one-shot initializer has already fired on this instance.
>
> The front-running window is closed on this instance; residual risk is limited to a future re-deploy of the same code with the same non-atomic initialization.

> **This finding is closed by observation.** The unsigned probe above reached an already-initialized guard of each listed entrypoint: the front-running window described in tier C has already closed on this instance. It stays in the document as the re-deploy risk it still is — see Elevation.1.R.1 — not as an open issue.

### Declared gaps

#### **S**poofing

**Declared gap — not derivable from the bytecode.** The call graph shows *whether* a path reaches `require_auth`; never *who* the verified `Address` is, who holds that address's key, nor how the client that builds the transaction authenticates the user. Identity lives outside the contract. Measured on soroguard's calibration corpus (75 mainnet contracts, `docs/CALIBRACAO.md`): zero derivable findings in 100% of them — this is a structural limit of bytecode analysis, not a detector failure on this contract. **Requires manual review of the off-chain flow:** custody of the privileged keys (multisig or a single key?), authentication of the frontend/backend that signs, and whether any address with an administrative role is a shared account. **Where identity is asserted in this contract:** 38 invocable entrypoints reach `require_auth*`. **Access control (18 admin-shaped, key-holder trace required):** `admin_set_rewards_state`, `apply_transfer_ownership`, `apply_upgrade`, `commit_transfer_ownership`, `commit_upgrade`, `kill_claim` and 12 more — those are the calls whose `Address` is a privileged role and has to be traced back to a key holder: custody, multisig or a single key. **Self-authorization (20 user-shaped, no key-holder trace):** `adjust_total_accumulated_reward`, `claim`, `claim_all_position_fees`, `claim_position_fees`, `claim_protocol_fees`, `deposit` and 14 more — there `require_auth` is the caller authorizing their own address (`swap`, `deposit`, `withdraw` are the usual shape), which is a user operation, not access control; there is no privileged key behind it to trace. **This split is a name-shape heuristic (tier C), not a bytecode fact** — the bytecode shows that `require_auth*` is reached, never *whose* address is authorized. Check it against the signatures before using the split as a work list. The module does not export `__check_auth`, so the signature check is the host's, not this contract's.

#### **T**ampering

**Declared gap — no finding derived for this contract.** Three detectors feed this letter and none fired: `write-before-auth` (compares bytecode offsets *within the same body*), `host-prng-in-value-path` (host PRNG on a path that changes state or calls out) and `vulnerable-sdk` when the advisory is not about authorization. The write-before-auth detector compares offsets *inside a single body* and found no body where a write precedes the first `require_auth`. It cannot see the ordering when the write and the authorization live in different functions, so this is absence of signal, not a demonstration that the ordering is correct. Watch the classification: state tampering through **missing** authorization lives under **Elevation** (5 findings), not here. **What still requires manual review:** validation of the arguments entering the entrypoints, and trust in data coming from another contract (oracle, router) — neither is derivable from reachability.

#### **I**nformation Disclosure

**Declared gap — not derivable from the bytecode.** All ledger state in Soroban is public by construction, so "excessive disclosure" is a question about *which data the protocol chose to put on-chain* — a product decision the WASM does not record. The bytecode also does not say what the arguments and the event topics mean. Measured on soroguard's calibration corpus (75 mainnet contracts, `docs/CALIBRACAO.md`): zero derivable findings in 100% of them — a structural limit. **Requires manual review:** which fields go into storage and into event topics, and whether any of them is data that should not be public or that gives an advantage to whoever reads the ledger before the transaction settles. **The concrete surface to review in this contract:** inferred storage keys `ChunkBitmap`, `ClaimKilled`, `Fee`, `FeeGrowthGlobal0X128`, `FeeGrowthGlobal1X128`, `FullRangeLiquidity` and 23 more; declared event topics `claim_fees`. Those are the fields that end up readable on a public ledger.

#### **D**enial of Service

**Declared gap — no finding derived for this contract.** The detector for this letter is `archival-risk`. 90 exports reach the `extend_*_ttl` family, the nearest at 2 hops (`adjust_total_accumulated_reward`, `admin_set_rewards_state`, `apply_transfer_ownership`, `apply_upgrade`, `backfill_plane_data`, `claim` and 84 more), and the detector only fires when none does. **Read that count as an over-approximation:** *reaching* is not *executing* — measured on the calibration corpus, ~84% of the bare positives go through a shared helper the entrypoint never runs (`docs/CALIBRACAO.md`), and read-only entrypoints land in this list for exactly that reason. If those renewal paths do not run during the contract's normal operation, the archival risk still exists and the bytecode does not show it. **What is not derivable from the call graph:** resource exhaustion from large input (ledger CPU/memory limits), dependency on the liveness of a contract reached through `call`, and an administrative `pause`/`kill` able to freeze the system. These require manual review.

---

## What are we going to do about it?

**This whole section is tier C (inference).** Remediation is not a bytecode fact: it is a proposal, derived from the finding and from the concrete entrypoint that produced it, and it needs review by someone who knows the system's design. None of them has been applied or verified — the tool reads the binary that is in production today.

| Threat | Issues |
|---|---|
| **S**poofing | _No remediation: no threat was derived under this letter (see the declared gap in the previous section)._ Remediation has to be written together with the threat, after manual review — filling this in without the finding would produce a fix with no matching problem. **The work this letter leaves for the review:** review who holds the keys behind the 18 admin-shaped entrypoints that reach `require_auth*` listed in the Spoofing gap (`admin_set_rewards_state`, `apply_transfer_ownership`, `apply_upgrade`, `commit_transfer_ownership`, `commit_upgrade`, `kill_claim` and 12 more) — custody, multisig or single key; the 20 user-shaped ones listed there (`adjust_total_accumulated_reward`, `claim`, `claim_all_position_fees`, `claim_position_fees`, `claim_protocol_fees`, `deposit` and 14 more) authorize the caller's own address and need no key-holder trace. |
| **T**ampering | _No remediation: no threat was derived under this letter (see the declared gap in the previous section)._ Remediation has to be written together with the threat, after manual review — filling this in without the finding would produce a fix with no matching problem. **The work this letter leaves for the review:** review argument validation and trust in data arriving from contracts reached through `call`/`try_call` in the 25 entrypoints listed in the Tampering gap (`admin_set_rewards_state`, `apply_upgrade`, `backfill_plane_data`, `claim`, `claim_all_position_fees`, `claim_position_fees` and 19 more) — the write-before-auth detector only compares offsets inside one body and cannot answer either question. |
| **R**epudiation | **Repudiate.1.R.1** [C] — Emit `contract_event` in the entrypoints that today reach a write without reaching an event: `adjust_total_accumulated_reward`, `admin_set_rewards_state`, `estimate_swap`, `estimate_swap_strict_receive`, `estimate_working_balance`, `gauges_get_reward_info`, `get_rewards_info`, `get_total_accumulated_reward`, `get_total_claimed_reward`, `get_total_configured_reward`, `get_unused_reward`, `get_user_reward` and 9 more. With no topic emitted there is no `getEvents` filter able to detect the action, so those mutations stay outside *event-based* monitoring; they are only detectable by state diff (`getLedgerEntries`) or by watching the instance wasm hash. No monitor of that kind is in the sibling plan either, so today the action leaves no trail an off-chain observer can follow.<br><br>**Repudiate.1.R.2** [C] — In the event of each of those entrypoints (`adjust_total_accumulated_reward`, `admin_set_rewards_state`, `estimate_swap`, `estimate_swap_strict_receive`, `estimate_working_balance`, `gauges_get_reward_info`, `get_rewards_info`, `get_total_accumulated_reward`, `get_total_claimed_reward`, `get_total_configured_reward`, `get_unused_reward`, `get_user_reward` and 9 more), the first topic has to be a fixed symbol — one per action, not a value derived from an argument — because that is what the monitoring plan's `getEvents` filters on. Which symbol to use is a decision for whoever writes the contract: soroguard cannot propose a topic name, only state that without a stable topic the detection rule for those mutations is impossible to write. |
| **I**nformation Disclosure | _No remediation: no threat was derived under this letter (see the declared gap in the previous section)._ Remediation has to be written together with the threat, after manual review — filling this in without the finding would produce a fix with no matching problem. **The work this letter leaves for the review:** review the 29 inferred storage keys (`ChunkBitmap`, `ClaimKilled`, `Fee`, `FeeGrowthGlobal0X128`, `FeeGrowthGlobal1X128`, `FullRangeLiquidity` and 23 more) and the 1 declared event topic (`claim_fees`) listed in the Information-disclosure gap, and decide which of those fields should not be readable on a public ledger or gives an advantage to whoever reads the ledger first. |
| **D**enial of Service | _No remediation: no threat was derived under this letter (see the declared gap in the previous section)._ Remediation has to be written together with the threat, after manual review — filling this in without the finding would produce a fix with no matching problem. **The work this letter leaves for the review:** check whether the 90 renewal paths listed in the DoS gap (`adjust_total_accumulated_reward`, `admin_set_rewards_state`, `apply_transfer_ownership`, `apply_upgrade`, `backfill_plane_data`, `claim` and 84 more) actually run during normal operation — *reaching* `extend_*_ttl` is not *executing* it, plus resource exhaustion from large input and the liveness of contracts reached through `call`. |
| **E**levation of Privilege | **Elevation.1.R.1** [C] — Before touching the code, confirm in the source of `init_pools_plane` whether the write is actually executed — shortest path to the write: `fn#384 → fn#242 → fn#195 → fn#77 → put_contract_data`. If the write branch belongs to a shared helper that `init_pools_plane` never executes, Elevation.1 is a **false positive and should be closed**, not remediated.<br><br>**Elevation.1.R.2** [C] — Move `init_pools_plane`'s initialization into `__constructor`. In Soroban the constructor runs in the same transaction as the deployment, which closes the front-running window by construction — instead of depending on the operator managing to initialize before a mempool watcher.<br><br>**Elevation.1.R.3** [C] — While `init_pools_plane` keeps existing as a public entrypoint, make it abort when the instance key is already written (the inferred keys are in *Inferred data stores*), and cover it with a test that checks the second call fails. A guard without a test does not count as remediation — it is the assumption that the guard exists. Verify in the source whether re-initialization is already guarded; if it is, close this finding with that justification recorded.<br><br>**Elevation.2.R.1** [C] — Before touching the code, confirm in the source of `initialize` whether the write is actually executed — shortest path to the write: `fn#385 → fn#236 → fn#240 → put_contract_data`. If the write branch belongs to a shared helper that `initialize` never executes, Elevation.2 is a **false positive and should be closed**, not remediated.<br><br>**Elevation.2.R.2** [C] — Move `initialize`'s initialization into `__constructor`. In Soroban the constructor runs in the same transaction as the deployment, which closes the front-running window by construction — instead of depending on the operator managing to initialize before a mempool watcher.<br><br>**Elevation.2.R.3** [C] — While `initialize` keeps existing as a public entrypoint, make it abort when the instance key is already written (the inferred keys are in *Inferred data stores*), and cover it with a test that checks the second call fails. A guard without a test does not count as remediation — it is the assumption that the guard exists. Verify in the source whether re-initialization is already guarded; if it is, close this finding with that justification recorded.<br><br>**Elevation.3.R.1** [C] — Before touching the code, confirm in the source of `initialize_all` whether the write is actually executed — shortest path to the write: `fn#386 → fn#236 → fn#240 → put_contract_data`. If the write branch belongs to a shared helper that `initialize_all` never executes, Elevation.3 is a **false positive and should be closed**, not remediated.<br><br>**Elevation.3.R.2** [C] — Move `initialize_all`'s initialization into `__constructor`. In Soroban the constructor runs in the same transaction as the deployment, which closes the front-running window by construction — instead of depending on the operator managing to initialize before a mempool watcher.<br><br>**Elevation.3.R.3** [C] — While `initialize_all` keeps existing as a public entrypoint, make it abort when the instance key is already written (the inferred keys are in *Inferred data stores*), and cover it with a test that checks the second call fails. A guard without a test does not count as remediation — it is the assumption that the guard exists. Verify in the source whether re-initialization is already guarded; if it is, close this finding with that justification recorded.<br><br>**Elevation.4.R.1** [C] — Before touching the code, confirm in the source of `initialize_boost_config` whether the write is actually executed — shortest path to the write: `fn#387 → fn#229 → fn#231 → fn#235 → put_contract_data`. If the write branch belongs to a shared helper that `initialize_boost_config` never executes, Elevation.4 is a **false positive and should be closed**, not remediated.<br><br>**Elevation.4.R.2** [C] — Move `initialize_boost_config`'s initialization into `__constructor`. In Soroban the constructor runs in the same transaction as the deployment, which closes the front-running window by construction — instead of depending on the operator managing to initialize before a mempool watcher.<br><br>**Elevation.4.R.3** [C] — While `initialize_boost_config` keeps existing as a public entrypoint, make it abort when the instance key is already written (the inferred keys are in *Inferred data stores*), and cover it with a test that checks the second call fails. A guard without a test does not count as remediation — it is the assumption that the guard exists. Verify in the source whether re-initialization is already guarded; if it is, close this finding with that justification recorded.<br><br>**Elevation.5.R.1** [C] — Before touching the code, confirm in the source of `initialize_rewards_config` whether the write is actually executed — shortest path to the write: `fn#388 → fn#233 → fn#235 → put_contract_data`. If the write branch belongs to a shared helper that `initialize_rewards_config` never executes, Elevation.5 is a **false positive and should be closed**, not remediated.<br><br>**Elevation.5.R.2** [C] — Move `initialize_rewards_config`'s initialization into `__constructor`. In Soroban the constructor runs in the same transaction as the deployment, which closes the front-running window by construction — instead of depending on the operator managing to initialize before a mempool watcher.<br><br>**Elevation.5.R.3** [C] — While `initialize_rewards_config` keeps existing as a public entrypoint, make it abort when the instance key is already written (the inferred keys are in *Inferred data stores*), and cover it with a test that checks the second call fails. A guard without a test does not count as remediation — it is the assumption that the guard exists. Verify in the source whether re-initialization is already guarded; if it is, close this finding with that justification recorded. |

Letters with no remediation because they have no derived threat: Spoof, Tamper, Info, DoS. This is not "nothing to do" — it is "not derivable from bytecode analysis", and the corresponding work is described in each declared gap of the previous section.

---

## Did we do a good job?

The template's five questions are answered below with what automatic generation can support. Where the answer depends on the team's process rather than on the binary, that is stated instead of assumed.

**Has the data flow diagram been referenced since it was created?**

**The tool cannot answer this one.** The diagram was generated together with this document, in the same run (2026-09-17); whether the team goes back to it afterwards is a process fact, outside the binary, and whoever reviews has to record it. **What is verifiable here is the dependency in the other direction:** the diagram's 4 trust boundaries over 96 `process` nodes are what the threat table is organized by — each `process` node is a module export, the open boundary (no reachable `require_auth*`) is the group the Elevation findings come from, and the cross-call boundary is what the Tampering gap points at. So the threats are derived from the diagram's content, not merely accompanied by it.

**Did the STRIDE model uncover any new design issues or concerns that had not been previously addressed or thought of?**

The tool has no memory of what the team already knew, so it cannot say what is *new* — only what it derived: 6 threats across 2 STRIDE letters, from 96 invocable entrypoints. Marking which ones were unknown is the reviewer's job. Point of attention: 5 of them depend on source confirmation before counting as a finding (Elevation.1, Elevation.2, Elevation.3, Elevation.4, Elevation.5).

**Did the treatments identified in the "What are we going to do about it" section adequately address the issues identified?**

**Not verified, and not verifiable by this tool.** Every remediation in the previous section is tier C and is proposed, not applied. soroguard read the WASM that is deployed (hash `12fca5a7a96577273b6d4184cf9c984036cda0e8f0594747e7b2933dced37ee6`) and cannot observe a fix that has not reached the ledger. **The objective test is concrete:** run soroguard again against the contract after the redeploy and check that the corresponding finding disappears — if it persists, the fix did not reach the path that produced the evidence.

**Have additional issues been found after the threat model?**

Outside the scope of this run — it depends on audits, manual review and incidents after 2026-09-17. Recorded here as the most likely places for open issues to surface: the letters declared as gaps (Spoof, Tamper, Info, DoS), and the threats flagged for human confirmation. This follow-up section must be updated by whoever reviews it, not regenerated.

**Any additional thoughts or insights on the threat modeling process that could help improve it next time?**

  - The analysis is of **deployed bytecode**, and the asymmetry matters: "does not reach `require_auth`" is a sound negative for this module's call graph — it says nothing about authorization enforced inside a contract reached through `call`/`try_call`, nor about `__check_auth`; "reaches `put_contract_data`" is an over-approximation, because the write may sit on a branch the entrypoint never executes. Every positive finding carries its hop count for that reason.
  - This module's call graph is complete (no `call_indirect`), so the negative claims in this document are sound with respect to that graph — and only to it.
  - Spoofing and Information Disclosure do not come out of the bytecode: they depend on identity and on a product decision about which data goes onto a public ledger, and neither is in the binary. Measured on the calibration corpus: zero findings in 100% of the 75 mainnet contracts (`docs/CALIBRACAO.md`). Filling those two letters with generic text to satisfy the template's "≥1 per letter" is what would get the document discarded by the first competent reviewer. What this run hands the manual review for those two letters, in numbers: 38 of 96 invocable entrypoints reaching `require_auth*`, 29 inferred storage keys and 1 declared event topic.
  - The window was observed (40160 ledgers, ~61.76 h, 42457 events across 9 topics), but **no monitor in the sibling plan could anchor a baseline on it** — no monitor binds an observed topic — the plan's rules filter on topics that did not appear in the window. Tier B here is a record of the window, not a baseline: every threshold in the monitoring plan still has to declare that it has no observed basis.
  - What this model does **not** cover, by construction: economic design (incentives, settlement, oracle), governance and custody of the privileged keys, security of the frontend and of the infrastructure that builds the transactions, and whether the address authorized in each `require_auth` is the right address. None of those questions can be answered from the binary.

### Submission verdict

**NEEDS INPUT.** The tool's own checks pass; 5 items below are input that no bytecode or on-chain analysis produces. Filling them is what makes this document submittable — nothing in the analysis has to change.

### Input the team must provide before submitting

None of these comes out of a binary or out of the chain. Each line names the worksheet it is filled from; the analysis above does not change when they are answered.

1. Write section 1, "What are we working on?": what the protocol does, who the actors are, what value it holds in custody and which trust assumptions live off-chain. Worksheet: the *Analyzed object* table and the measured surface right below the gap notice in section 1 — the bytecode records none of that, so no analysis closes this one.
2. STRIDE letter Spoof has no issue — the template requires at least one; fill it from the worksheet in the Spoof gap section (it lists the concrete surface to review).
3. STRIDE letter Tamper has no issue — the template requires at least one; fill it from the worksheet in the Tamper gap section (it lists the concrete surface to review).
4. STRIDE letter Info has no issue — the template requires at least one; fill it from the worksheet in the Info gap section (it lists the concrete surface to review).
5. STRIDE letter DoS has no issue — the template requires at least one; fill it from the worksheet in the DoS gap section (it lists the concrete surface to review).
