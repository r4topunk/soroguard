# Public site URL (no trailing slash). The ONE place the domain lives: used by index.html for og:url / og:image / twitter:image.
# Override per environment with a VITE_SITE_URL env var (e.g. in Vercel project settings).
VITE_SITE_URL=https://soroguard.r4to.com
